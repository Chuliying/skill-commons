// Regression fixture for portable gate exclusion filters. These findings are
// intentionally inside a test file and must not fail production-source gates.
const captured: any = 'text-[#333333]'

console.log(captured)
